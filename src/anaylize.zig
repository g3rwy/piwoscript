//TODO
//- Check if ELSE has IF node before it
//- Transform UTF-8 polish letters to unused ascii ones
//- Technically i can check expressions too, if the types are correctly used
//- Transform ID's to indexes
//- Create Value union
